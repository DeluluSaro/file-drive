/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {
                hostname:'giddy-minnow-853.convex.cloud'
            }
        ]
    }
};

export default nextConfig;
